# Бодрый Боцман — внутренняя система управления

Веб-приложение для управления морскими прогулками, флотом и тюнинг-центром
компании «Бодрый Боцман». Система объединяет рейсы, расчёты с инвесторами,
зарплаты, заказы, задачи команды, складской учёт и финансовую аналитику.

Приложение написано на Flask, использует SQLite и серверные Jinja-шаблоны.
Интерфейс адаптирован для мобильных устройств и может устанавливаться как PWA.

## Возможности

### Зарплаты

- учёт выполненных работ, часов, ставок и начислений;
- недельные периоды и фильтр по сотруднику;
- отметка выплаты зарплаты;
- автоматическое создание начислений из рейсов и выполненных задач;
- гарантированный минимум за смену;
- расчёт гонорара менеджера по продажам;
- отправка выплат самозанятым через Т-Банк.

### Рейсы и инвесторы

- учёт катера, времени, вида рейса, канала продажи и выручки;
- несколько сотрудников в одном рейсе;
- учёт топлива, стоянки и дополнительных расходов;
- автоматический расчёт комиссии, остатка и выплаты инвестору;
- формирование договоров в DOCX;
- отдельный кабинет инвестора с данными только по его катерам.

Расчёт рейса:

```text
Комиссия       = Выручка × Процент комиссии
Прямые расходы = Оплата команды + Топливо + Стоянка + Прочие расходы
Остаток        = Выручка − Комиссия − Прямые расходы
Инвестору      = Остаток / 2
Компании       = Комиссия + Остаток / 2
```

### Импорт из Yclients

- загрузка записей и групповых событий за выбранный период;
- определение катера по цвету записи;
- сопоставление услуг с видами рейсов;
- распознавание прямых продаж и агрегаторов;
- объединение записей одного рейса с несколькими сотрудниками;
- присоединение поздно появившейся служебной записи второго сотрудника к уже
  импортированному рейсу по катеру, дате и точному времени;
- перенос зарплатных начислений при переназначении команды в Yclients без
  повторного учёта выручки;
- защита от повторного импорта;
- очередь проверки перед созданием рейса;
- автоматическая проверка реального графика смен Yclients и доплат до
  минимальной ставки, включая смены без единого рейса.

Импорт не создаёт обычный рейс без проверки: кандидат сначала попадает в
очередь, где администратор может исправить данные, подтвердить или пропустить
его.

### Флот

- карточка каждого катера;
- расчётный остаток топлива и журнал заправок;
- автоматическое списание топлива по завершённым групповым рейсам YCLIENTS;
- ручное исключение ошибочных списаний без их повторного импорта из YCLIENTS;
- очередь индивидуальных рейсов для ручного указания расхода;
- хранение судовых документов;
- предрейсовые и послерейсовые чек-листы;
- фотографии обнаруженных проблем;
- реестр дефектов и их статусы;
- назначение устранения дефекта сотруднику;
- преобразование выполненной задачи в начисление зарплаты;
- офлайн-журнал капитана с чек-листами, неисправностями и документами.

### Офлайн-режим капитана

В личном кабинете капитана раздел «Флот» содержит переход в «Судовой
журнал». При первом открытии с интернетом PWA сохраняет интерфейс, список
судов и актуальные шаблоны чек-листов. После этого журнал открывается без
связи и позволяет:

- полностью пройти предрейсовый или послерейсовый осмотр;
- сохранить комментарии и фотографии проблем;
- сообщить о ручной неисправности;
- открыть заранее скачанные документы выбранного судна.

Черновики, фотографии и очередь отправки хранятся в IndexedDB браузера.
Документы сохраняются отдельно через кнопку «Скачать документы на
устройство». При появлении связи очередь отправляется при открытии/возврате
в приложение, вручную и через Background Sync в поддерживающих его
браузерах.

Каждая операция имеет клиентский UUID. Маршрут `/api/offline/sync` сохраняет
чек-лист или неисправность одной транзакцией и запоминает UUID в
`offline_operations`, поэтому повтор после обрыва ответа не создаёт дубль и
не отправляет повторные уведомления. Если авторизационная сессия истекла,
очередь остаётся на устройстве до следующего входа. Выход из кабинета
очищает локальные рабочие данные и документы этого браузера.

### Тюнинг-центр

- клиентская база и заказы на обслуживание лодок;
- состав заказа: работы, товары, себестоимость, наценка и скидка;
- статусы заказа и отдельных работ;
- назначение работ сотрудникам;
- фотографии выполнения и внутренние заметки;
- напоминания по заметкам;
- интерактивные листы диагностики корпуса;
- автоматическое создание финансового проекта для каждого заказа;
- публичный кабинет клиента по уникальной ссылке;
- согласование клиентом отдельных работ;
- формирование актов в PDF и документов передачи.

Заявки с сайта на Tilda могут автоматически создавать новые заказы со
статусом «Новая заявка».

### Оплаты и чеки

- ручная регистрация платежей по заказу;
- онлайн-оплата через ЮKassa;
- обработка webhook ЮKassa;
- сверка экскурсионных записей YCLIENTS с успешными платежами ЮKassa;
- полные и частичные возвраты по рейсам с идемпотентной защитой;
- фискальные чеки частичного возврата и неизменяемый журнал операций;
- фискализация ручных платежей через МодульКассу;
- повторная отправка и проверка статуса чека;
- учёт оплаченной суммы и остатка по заказу.

### Аналитика

- импорт банковских операций из Т-Банка;
- ручное добавление операций, отсутствующих в выписке;
- назначение операции на проект или отдельную работу;
- распределение одной операции между несколькими проектами;
- расчёт доходов, расходов и прибыли проекта;
- включение в себестоимость зарплаты и списанных материалов;
- детализация рентабельности отдельных работ заказа.

### Склад и снабжение

- несколько складов;
- каталог материалов и товаров;
- приход, остатки и списания;
- единицы учёта: штуки, квадратные и погонные метры;
- фотографии товаров;
- минимальный остаток и автоматические заявки;
- заявки сотрудников на снабжение;
- списание материалов непосредственно из назначенной задачи;
- автоматическое списание товара, добавленного в заказ тюнинг-центра.

### Уведомления и автоматизация

- уведомления администратора и сотрудников через Telegram;
- браузерные push-уведомления через Web Push/VAPID;
- напоминания по заказам;
- cron-проверка смен капитанов;
- cron-проверка статусов чеков и отправка напоминаний.

### Сотрудники

- единый список сотрудников в административном интерфейсе;
- назначение и удаление должностей без редактирования исходного кода;
- получение личных Telegram-аккаунтов из сообщений боту;
- привязка, проверка и удаление персонального канала уведомлений;
- автоматический перенос ранее настроенных Telegram Chat ID.

## Роли и доступ

В приложении три защищённых кабинета и один клиентский интерфейс:

| Роль | Адрес | Доступ |
| --- | --- | --- |
| Администратор | `/admin/login` | Все операционные и финансовые разделы |
| Сотрудник | `/team/login` | Собственная зарплата, задачи, документы, чек-листы и заявки |
| Инвестор | `/investor/login` | Рейсы и выплаты только по связанным катерам |
| Клиент | `/client/<token>` | Свой заказ, согласование работ и диагностика |

Сессии подписываются ключом `SECRET_KEY`. Пароли хранятся в базе в виде
хешей Werkzeug. Начальные аккаунты задаются в `app.py`, а справочники флота —
в `modules/fleet/constants.py`; данные синхронизируются с базой при запуске.

## Как связаны основные данные

```text
Yclients → кандидат импорта → рейс → работа команды → зарплата
                              └────→ расчёт выплаты инвестору

Tilda/администратор → заказ тюнинг-центра → финансовый проект
                           ├→ работы → задачи сотрудников → зарплата
                           ├→ материалы/товары → складские списания
                           ├→ платежи → ЮKassa/МодульКасса
                           └→ банковские операции → прибыль проекта
```

## Технологии

- Python 3.11;
- Flask 2.2;
- SQLite;
- Jinja2 и обычный JavaScript;
- Requests для внешних API;
- ReportLab для PDF;
- python-docx для DOCX;
- pywebpush для push-уведомлений;
- Gunicorn или Phusion Passenger для production.

## Структура репозитория

```text
.
├── app.py                         # основное Flask-приложение и схема БД
├── integrations/
│   └── telegram.py                # клиент Telegram Bot API
├── modules/
│   ├── employees/                 # сотрудники, должности и Telegram
│   │   ├── routes.py              # административный Blueprint
│   │   ├── services.py            # правила привязки и должностей
│   │   ├── repository.py          # SQL-запросы модуля
│   │   └── constants.py           # данные для первого запуска
│   ├── fleet/                     # изолированный модуль флота
│   │   ├── routes.py              # Blueprint и HTTP-маршруты
│   │   ├── services.py            # правила чек-листов и неисправностей
│   │   ├── repository.py          # SQL-запросы модуля
│   │   ├── fuel_services.py       # расчёт топлива и синхронизация YCLIENTS
│   │   ├── fuel_repository.py     # журнал топлива и очередь рейсов
│   │   └── constants.py           # катера, статусы и вопросы осмотра
│   ├── offline/                   # офлайн-журнал и синхронизация капитана
│   │   ├── routes.py              # экран, bootstrap-снимок и sync API
│   │   ├── services.py            # валидация и атомарное воспроизведение
│   │   └── repository.py          # UUID операций и схема очереди сервера
│   └── refunds/                   # возвраты по экскурсионным рейсам
│       ├── routes.py              # административный Blueprint
│       ├── services.py            # сверка, чеки и безопасные возвраты
│       └── repository.py          # платежи, связи и журнал возвратов
├── passenger_wsgi.py              # точка входа для Beget/Passenger
├── requirements.txt               # Python-зависимости
├── runtime.txt                    # версия Python для хостинга
├── templates/                     # Jinja-шаблоны всех кабинетов
├── static/                        # стили, PWA, изображения и шаблон договора
├── tests/                          # интеграционные проверки модулей
└── scripts/
    ├── import_moysklad_stock.py   # импорт складского каталога
    └── moysklad_import.json       # подготовленные данные импорта
```

Приложение постепенно переводится на модульную архитектуру. Флот, сотрудники,
офлайн-синхронизация и возвраты оформлены как Flask Blueprints с отдельными
HTTP-, сервисными и SQL-слоями; остальные разделы пока остаются в `app.py`. При импорте приложения вызывается
`init_db()`: функция создаёт отсутствующие таблицы, добавляет новые поля и
выполняет необходимые обратные заполнения данных.

## Локальный запуск

Требуется Python 3.11.

```bash
git clone https://github.com/bodrbo/-.git bodrbo-app
cd bodrbo-app
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

Приложение будет доступно по адресу <http://127.0.0.1:5000>.

При первом запуске рядом с `app.py` создастся `workhours.db`. Существующая
база будет автоматически обновлена встроенными миграциями.

## Переменные окружения

Для локальной разработки можно экспортировать переменные в shell. На Beget
они загружаются из файла `.env` рядом с `passenger_wsgi.py`.

### Основные

| Переменная | Назначение |
| --- | --- |
| `SECRET_KEY` | Подпись пользовательских сессий |
| `WORKHOURS_DB_PATH` | Необязательный путь к SQLite-файлу; используется для изоляции тестов |
| `YCLIENTS_PARTNER_TOKEN` | Партнёрский токен Yclients |
| `YCLIENTS_USER_TOKEN` | Пользовательский токен Yclients |
| `YCLIENTS_COMPANY_ID` | ID филиала Yclients |
| `CRON_SECRET` | Защита внутренних cron-эндпоинтов |

### Платежи и банк

| Переменная | Назначение |
| --- | --- |
| `YOOKASSA_SHOP_ID` | ID магазина ЮKassa |
| `YOOKASSA_SECRET_KEY` | Секретный ключ ЮKassa |
| `YOOKASSA_EXCURSION_VAT_CODE` | Код НДС для чека частичного возврата по рейсу; по умолчанию `7` |
| `YOOKASSA_EXCURSION_PAYMENT_MODE` | Признак способа расчёта для чека; по умолчанию `full_prepayment` |
| `TBANK_API_TOKEN` | Доступ к выписке Т-Банка |
| `TBANK_API_TOKEN_PAYMENT` | Создание выплат самозанятым |
| `TBANK_ACCOUNT_NUMBER` | Номер расчётного счёта |
| `MODULKASSA_USERNAME` | API-логин МодульКассы |
| `MODULKASSA_PASSWORD` | API-пароль МодульКассы |
| `MODULKASSA_ENV` | `production` для боевого контура |

### Интеграции и уведомления

| Переменная | Назначение |
| --- | --- |
| `TILDA_WEBHOOK_SECRET` | Проверка webhook от Tilda |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram-бота |
| `TELEGRAM_BOT_USERNAME` | Необязательное имя бота без `@` для ссылки из раздела сотрудников |
| `TELEGRAM_CHAT_ID` | Основной административный чат |
| `TELEGRAM_APPROVAL_CHAT_ID` | Чат уведомлений о согласованиях |
| `VAPID_PUBLIC_KEY` | Публичный ключ Web Push |
| `VAPID_PRIVATE_KEY` | Приватный ключ Web Push |
| `VAPID_CLAIMS_EMAIL` | Контакт для VAPID claims |

Пример `.env` без реальных секретов:

```dotenv
SECRET_KEY=replace-with-a-long-random-value
YCLIENTS_PARTNER_TOKEN=...
YCLIENTS_USER_TOKEN=...
YCLIENTS_COMPANY_ID=...
CRON_SECRET=...
YOOKASSA_SHOP_ID=...
YOOKASSA_SECRET_KEY=...
YOOKASSA_EXCURSION_VAT_CODE=7
YOOKASSA_EXCURSION_PAYMENT_MODE=full_prepayment
```

Не добавляйте `.env`, токены, пароли, банковские реквизиты или рабочую базу
данных в Git.

## Production

Репозиторий содержит два варианта запуска:

```bash
gunicorn app:app
```

или через Phusion Passenger, который импортирует объект `application` из
`passenger_wsgi.py`.

На текущем Beget-хостинге фактический Passenger loader может находиться
уровнем выше каталога репозитория. При изменении логики загрузки `.env`
необходимо синхронно обновлять и серверный loader.

### Возвраты по экскурсионным рейсам

Экран доступен из раздела «Рейсы и инвесторы». Кнопка «Обновить сверку»
получает клиентские записи YCLIENTS за выбранный период, а также успешные
платежи и возвраты из ЮKassa. `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY`
должны относиться именно к тому магазину, в который поступают оплаты
экскурсий; реквизиты другого магазина не позволят найти нужные платежи.

YCLIENTS передаёт признаки оплаты (`paid_full`, `prepaid`), но карточка записи
не содержит ID платежа ЮKassa. Поэтому система автоматически связывает
платёж только при наличии точного ID записи в его metadata. Остальные платежи
администратор связывает вручную после проверки суммы, даты, описания и
последних цифр карты. Совпадение одной лишь суммы связью не считается.

Полный возврат отправляется без нового объекта чека: ЮKassa формирует его по
исходному платежу. Для частичного возврата система передаёт email клиента и
отдельный чек возврата. Перед первым боевым частичным возвратом необходимо
сверить `YOOKASSA_EXCURSION_VAT_CODE` и `YOOKASSA_EXCURSION_PAYMENT_MODE` с
настройками исходных экскурсионных чеков и бухгалтером.

Каждая операция сохраняется до API-запроса с уникальным ключом
идемпотентности. При сетевом тайм-ауте новый возврат не создаётся: система
сначала проверяет изменившуюся возвращённую сумму и повторяет тот же запрос
только тем же ключом. Автоматический повтор после 23 часов блокируется.

### Почасовая синхронизация YCLIENTS

На Beget необходимо раз в час вызывать защищённый маршрут. Существующий URL
топливной синхронизации сохранён, но теперь один вызов обновляет и рейсы с
доходами сотрудников/инвесторов, и топливный журнал:

```bash
curl -fsS "https://ВАШ-ДОМЕН/internal/cron/sync-fuel?token=ЗНАЧЕНИЕ_CRON_SECRET"
```

При временном сбое система на следующем успешном запуске повторно запрашивает
период от последней синхронизации. Повторное создание рейсов и списание
топлива исключены идентификаторами событий YCLIENTS. Первый автоматический
запуск проверяет последние семь дней, последующие — перекрывающийся период с
восстановлением разрыва после неуспешного запуска.

Перекрывающийся импорт также обновляет уже созданный рейс, если запись позже
исправили в YCLIENTS: синхронизируются состав экипажа, вид и длительность
рейса, выручка и комиссия, после чего пересчитываются зарплата и доли. Поэтому
сокращение индивидуальной аренды, например с двух часов до одного, не создаёт
дубликат и отражается при ближайшей синхронизации.

Красный цвет YCLIENTS `#f44336` означает отменённый рейс. Такие записи не
импортируются; если рейс успели загрузить до того, как его пометили красным,
следующая синхронизация удалит сам рейс, связанные начисления и автоматическое
списание топлива. При снятии красной отметки рейс снова может быть импортирован.

В доходы автоматически попадают только уже завершившиеся записи. Удалённые
записи, красные блокировки смен и записи со статусом «Не пришёл» пропускаются.
Рейс, который не удалось однозначно сопоставить с катером, сотрудником или
видом работы, остаётся в административной очереди «Ожидают подтверждения».

Для минимальной ставки синхронизация отдельно читает график работы каждого
активного сотрудника из Yclients. Поэтому рабочий день без клиентских записей
создаёт начисление «Доплата до минимальной ставки» на полные 3000 ₽. Если за
смену уже есть другие начисления, доплата уменьшается до разницы; если смену
сняли из графика, автоматически созданная доплата удаляется при следующей
успешной проверке.

Для каждого графика выполняется повторная попытка при временном сбое API.
Если отдельный сотрудник всё равно не ответил, остальные смены и доплаты
обрабатываются сразу, а начисления недоступного сотрудника не удаляются.
Ручной импорт показывает предупреждение с именами непроверенных сотрудников.

Синхронизация считает проведённым завершившийся рейс, если запись не удалена
и не имеет статуса «Не пришёл». Групповое событие списывается один раз вне
зависимости от количества клиентов и сотрудников внутри него. Обычная запись
попадает в очередь ручного расхода.

Учёт каждого катера начинается отдельно: первая заправка с отметкой «Бак
заправлен до полного» устанавливает остаток равным физической ёмкости. Рейсы,
начавшиеся раньше этого момента, в топливный журнал не попадают.

SQLite подходит для текущего небольшого внутреннего сервиса, но файл базы
должен находиться на постоянном диске и регулярно копироваться. Запуск
нескольких одновременно пишущих экземпляров приложения без миграции на
серверную СУБД не рекомендуется.

## Резервное копирование

Критичные пользовательские данные находятся в `workhours.db`. Кроме базы,
необходимо сохранять загруженные документы и фотографии в каталогах внутри
`static/`.

Перед обновлением production рекомендуется сделать копии:

```text
workhours.db
static/boat_documents/
static/work_photos/
static/checklist_photos/
static/supply_photos/
static/diplomas/
```

## Безопасность

- все секреты должны поступать только из окружения или серверного `.env`;
- production обязательно должен иметь постоянный `SECRET_KEY`;
- внутренние диагностические и cron-маршруты должны быть закрыты
  `CRON_SECRET`;
- перед публикацией изменений проверяйте репозиторий на случайно добавленные
  токены и пользовательские данные;
- при компрометации секрета его нужно отозвать у провайдера, а не только
  удалить из истории Git.
